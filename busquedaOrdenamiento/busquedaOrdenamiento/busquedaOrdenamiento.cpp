#include "controller.h"

/**
 * @brief Punto de entrada principal simplificado.
 */
int main() {
    // La lógica ha sido delegada al controlador para mantener el main limpio.
    iniciarAplicacion();
    return 0;
}